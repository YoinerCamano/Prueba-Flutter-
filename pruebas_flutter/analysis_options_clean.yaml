# Configuración de análisis excluyendo archivos legacy
include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - lib/data/legacy/**
    - lib/domain/legacy/**
    - lib/presentation/blocs/legacy/**
    - lib/presentation/pages/legacy/**
    - lib/presentation/widgets/legacy/**
    - build/**

linter:
  rules:
    # Reglas personalizadas si es necesario